# AGENTS.md

## 项目定位

这是“躬行启杭”交流平台，包含 React、TypeScript、Vite 前端和 FastAPI 后端。项目根目录同时管理前端、后端、数据和启动脚本。

开始修改前先阅读 `README.md`，然后根据任务查看：

- 前端入口：`frontend/src/main.tsx`
- 前端页面与组件：`frontend/src/pages/`、`frontend/src/components/`
- 后端入口：`backend/main.py`
- 后端路由：`backend/routes/`
- 配置与数据：`backend/config.py`、`backend/database.py`
- 启动流程：`scripts/start.bat`、`scripts/start.sh`

## 开发规则

- 前后端接口变更时，同时检查调用方和返回结构。
- 前端沿用现有 React、TypeScript、Vite 和 CSS 结构，不无故引入新框架。
- 后端保持 FastAPI 当前模块划分；为核心函数、复杂分支和不直观逻辑添加简洁中文注释，不逐行重复代码含义。
- 数据文件统一放在根 `data/`；相对配置路径以项目根目录为基准。
- `.env`、API Key、管理员密码和用户数据属于敏感内容，不读取、展示或提交真实值。
- 不提交数据库、上传文件、依赖目录或构建产物。
- 安装依赖、修改数据库结构、初始化数据、删除上传文件或连接远程服务前，先获得用户明确授权。

## 对话、分支与并行修改

同一个 Codex 项目中的多个对话共享当前工作目录和文件状态。分支只隔离 Git 版本路线，不会为不同对话创建独立文件：

- 同一工作目录中，无论存在多少分支，同一时间只允许一个对话修改代码。
- 其他对话可以同时进行只读检查、需求分析和方案设计，但开始修改前必须先确认当前 Git 状态。
- 一个修改任务完成后，先运行相关检查、总结改动并处理提交，再让另一个对话开始修改。
- 切换分支前，工作区应保持干净；不要带着来源不明的未提交改动切换任务。
- 只有为每个分支建立独立 Git worktree，并让不同对话分别使用不同目录时，才允许真正并行修改。
- 即使使用不同 worktree，涉及同一文件、接口或数据库结构的任务仍需提前协调。

每次用户提出一个新的代码或项目文件改动时，在开始修改前进行一次“大改动 / 小改动”判断：

- 判断只在该改动首次提出时执行一次，并简短告知用户结论以及应使用 `main` 还是功能分支。
- 同一改动后续的检查、修复、补充和验证过程中不重复判断，也不反复提醒。
- 不为这项判断单独创建 subagent；它属于当前对话开始任务时的固定检查。
- 只有用户后来明显扩大需求范围，使原判断依据发生实质变化时，才重新判断一次。
- 无法确定时按大改动处理，优先保护 `main` 的稳定性。

分支按修改风险使用，不按对话数量创建：

- 文案、局部样式、文档和单文件简单修复，可以在 `main` 完成。
- 只要涉及前后端联动、API、数据库、权限、安全、依赖、环境变量、文件存储、部署、多文件重构或不确定实验，就创建独立功能分支。
- `main` 应保持为当前经过验证、可以继续开发和部署的稳定版本。
- 功能分支完成并验证后再合并回 `main`；分支尚未合并时，不把其中的功能视为主线已有能力。

切换到另一个代码修改对话前，当前任务应尽量完成以下交接：

1. 停止继续扩大修改范围。
2. 运行 build、lint、语法检查或相关测试。
3. 查看并说明 Git 状态。
4. 更新必要的开发日志。
5. 创建与当前任务对应的提交。
6. 如果使用功能分支，明确它是否已经合并回 `main`。

## 验证

前端依赖可用时，在 `frontend/` 运行：

```bash
npm run build
npm run lint
```

后端修改后至少运行 Python 语法检查；涉及接口时还应启动服务并验证 `/api/health` 和受影响接口。

如果本机缺少依赖，不擅自安装，应说明哪些检查尚未运行。

结构调整、重要功能或技术决策完成后，在 `docs/dev-log.md` 记录修改内容、原因、验证结果、遗留问题和下一步。
